// Playlists synced in from HGC Radio carry this marker on playlist.source.
// They belong to the station rather than to one account, so every DJ and the
// owner sees them regardless of who they are attached to via djOwner.
export const HGC_SOURCE = "hgc";
